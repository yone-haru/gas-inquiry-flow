/**
 * 日次リマインド。100行単位のバッチ処理＋4分30秒での安全中断＋継続トリガーでの再開、
 * 期限超過エスカレーションを扱う（要件定義書 6.1章）。
 */

/**
 * 時間駆動トリガー本体（F-03）。Day3 で実装する。
 * setup.js の registerTriggers_() がこの関数名でトリガー登録するため、先に関数だけ用意しておく。
 */
function runDailyReminder() {
  // TODO(Day3, F-03): 未対応/対応中で期限が迫っている行をバッチ処理し、担当者ごとにまとめて通知する。
}
